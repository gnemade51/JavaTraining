package com.util.shape;

public class Calculation {
	public void add(int x, String...strings a)
	{
		int sum=0;
		for(int i-0;i<a.length;i++)
		{
			//sum=sum+a[i];
			//System.out.println(a[i]);
		}
	}
}